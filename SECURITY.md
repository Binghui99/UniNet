# Security policy

Please report vulnerabilities privately to the corresponding author using the email
listed in the paper. Do not open a public issue until a fix is available.

This project parses untrusted capture files. Run it with ordinary user privileges in
an isolated environment, limit input sizes, and do not assume a successfully parsed
capture is safe. The tool extracts metadata; it is not an intrusion prevention system.

