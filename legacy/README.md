# Legacy experiment artifacts

The original notebooks and scripts remain in the top-level `Task1_*`, `Task2_*`,
and `Task4-*` directories to preserve research provenance. They predate the
installable package, contain environment-specific paths, and are not exercised by
continuous integration. Use `src/uninet` and the `uninet` CLI for new experiments.
