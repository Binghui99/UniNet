# Legacy experiment artifacts

The original notebooks and scripts are preserved in `task1_notebooks/`,
`task2_notebooks/`, and `task4_original/` for research provenance. They predate the
installable package, contain environment-specific paths, and are not exercised by
continuous integration. Use the root `tmatrix.py` and four scripts under `tasks/`
for new experiments.
